@model IEnumerable<CxCSystem.Models.Asientos>

@{
    ViewBag.Title = "Asientos";
}

<h2>Asientos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("ID Asiento")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.Label("Cuenta debito")
        </th>
        <th>
            @Html.Label("Cuenta credito")
        </th>
        <th>
            @Html.Label("Monto Debito")
        </th>
        <th>
            @Html.Label("Monto Credito")
        </th>
        <th>
            @Html.Label("Cliente")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Asiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CuentaDB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CuentaCR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MontoDB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MontoCR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clientes.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
